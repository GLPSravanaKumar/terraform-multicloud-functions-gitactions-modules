[defaults]
# Path to your inventory directory (contains dev.ini, test.ini, qa.ini)
inventory = ./ansible_inventory_file.ini, ./inventories/dev/, ./inventories/test/, ./inventories/qa/
# Where roles are stored
roles_path = ./roles
remote_user = ubuntu
host_key_checking = False
private_key_file = "~/.ssh/id_ed25519_glpskumar"

[privilege_escalation]
become        = True