    - name: Install packges git,wget,tree,curl
      include_tasks: packages.yml
   
    - name: Install Terraform in TEST
      include_tasks: terraform.yml

    - name: Install Docker in TEST
      include_tasks: docker.yml

    - name: Install Python3 in TEST
      include_tasks: python3.yml
